<%= form_for(@private_message) do |f| %>
  <% if @private_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@private_message.errors.count, "error") %> prohibited this private_message from being saved:</h2>

      <ul>
      <% @private_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id, :value => session[:user_id] %>
  </div>
  <div class="field">
    <%= f.label :report_id %>
    <%= f.text_field :report_id, :value => params[:report_id] %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
